#include <stdio.h>
#include "stdbool.h"

int main(){

    printf("input:");
    char ch;
    while ((ch=getchar())!='#'){
        if(ch=='.')
            putchar('!');
        else if (ch=='!')
            printf("!!");
        else
            putchar(ch);
    }


}
