#include <stdio.h>
int main(){
    int n=0;
    for (int i = 0; i < 6; ++i) {
        for (int j = 0; j < i + 1; ++j) {
            putchar('A'+n);
            n++;
        }
        putchar('\n');
    }
}
