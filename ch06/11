#include <stdio.h>

int main() {
    int arrow[8];
    printf("Enter 8 integers: ");
    for (int i = 0; i < 8; ++i) {
        scanf("%d", &arrow[i]);
    }
    for (int i = 7; i >= 0; --i) {
        printf("%d ", arrow[i]);
    }

    return 0;
}
