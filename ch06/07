#include <stdio.h>
#include "string.h"
int main(){
    printf("Enter a word:");
    char word[20];
    scanf("%s",word);
    for (int i = 0; i < strlen(word)  ; ++i) {
        putchar(word[strlen(word)-i-1]);

    }
}
