#include <stdio.h>
#include "math.h"
/*
 * 编写一个程序,创建一个包含8个元素的int类型数组,
 * 分别把数组元素设置为2的前8次幂。使用for循环设置数组元素的值,
 * 使用do while循环显示数组元素的值
 */
int main() {
    int arrow[8];
    for (int i = 0; i < 8; ++i) {
        arrow[i]= pow(2,i);
    }
    int n=0;
    do {
        printf("%d ",arrow[n]);
        n++;
    } while (n<8);
}
